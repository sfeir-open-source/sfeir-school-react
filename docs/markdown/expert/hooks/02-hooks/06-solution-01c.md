<!-- .slide: class="center" -->

# Exercise 01
## Solution (Image details)

* `useEffect`: every time `url` prop changes
  * `url` in dependency array
  * ± `componentDidMount` and `componentDidUpdate`

* `useState` to hold details
  * `setState(apiResponse)` to hold API repsonse
  * use `state` variable to render thumbnails 